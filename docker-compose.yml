services:
  # Elasticsearch cluster
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
    container_name: freerange-elasticsearch
    environment:
      - node.name=es-node-1
      - cluster.name=freerange-cluster
      - discovery.type=single-node
      - bootstrap.memory_lock=true
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
      - xpack.security.enabled=false
      - xpack.security.enrollment.enabled=false
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - elasticsearch_data:/usr/share/elasticsearch/data
    ports:
      - "9200:9200"
      - "9300:9300"
    networks:
      - freerange-network
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://localhost:9200/_cluster/health || exit 1" ]
      interval: 30s
      timeout: 10s
      retries: 5

  # Redis for caching and queue
  redis:
    image: redis:7-alpine
    container_name: freerange-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - freerange-network
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 30s
      timeout: 10s
      retries: 5
    command: redis-server --appendonly yes

  # Notification Service
  notification-service:
    build: .
    image: freerange-notify:latest
    container_name: freerange-notification-service
    ports:
      - "${FREERANGE_SERVER_PORT:-8080}:8080"
    extra_hosts:
      - "localhost:host-gateway"
    depends_on:
      elasticsearch:
        condition: service_healthy
    env_file:
      - .env
    environment:
      - FREERANGE_DATABASE_URLS=${FREERANGE_DATABASE_URLS:-http://elasticsearch:9200}
      - FREERANGE_REDIS_HOST=${FREERANGE_REDIS_HOST:-redis}
      - FREERANGE_REDIS_PORT=${FREERANGE_REDIS_PORT:-6379}
      - FREERANGE_SERVER_PUBLIC_URL=${FREERANGE_SERVER_PUBLIC_URL:-http://localhost:8080}
    networks:
      - freerange-network
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:8080/v1/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  # Notification Worker (builds from the same Dockerfile as notification-service)
  notification-worker:
    build: .
    image: freerange-notify:latest
    container_name: freerange-notification-worker
    command: [ "./worker" ]
    depends_on:
      elasticsearch:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env
    environment:
      - FREERANGE_DATABASE_URLS=${FREERANGE_DATABASE_URLS:-http://elasticsearch:9200}
      - FREERANGE_REDIS_HOST=${FREERANGE_REDIS_HOST:-redis}
      - FREERANGE_REDIS_PORT=${FREERANGE_REDIS_PORT:-6379}
    healthcheck:
      test: [ "CMD-SHELL", "readlink /proc/1/exe | grep -q '/worker$' || exit 1" ]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - freerange-network
    restart: unless-stopped

  # UI: React Vite (TypeScript)
  ui:
    build:
      context: ./ui
      dockerfile: Dockerfile
    container_name: freerange-ui
    ports:
      - "3000:3000"
    environment:
      - NODE_OPTIONS=--max-old-space-size=1024
      - CHOKIDAR_USEPOLLING=${CHOKIDAR_USEPOLLING:-false}
      - CHOKIDAR_INTERVAL=${CHOKIDAR_INTERVAL:-1000}
      # API_PROXY_TARGET is server-side only (Vite proxy), not exposed to browser
      - API_PROXY_TARGET=http://notification-service:8080
      # Do NOT set VITE_API_BASE_URL here — browser must use /v1 (relative) to hit the proxy
    volumes:
      - ./ui:/app # Mount source code for hot reloading
      - /app/node_modules # Prevent overwriting node_modules
    networks:
      - freerange-network
    depends_on:
      - notification-service
    restart: unless-stopped

volumes:
  elasticsearch_data:
  redis_data:


networks:
  freerange-network:
    driver: bridge
